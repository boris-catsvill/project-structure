const config = {
  verbose: true,
  setupFilesAfterEnv: ['./jest-setup-files-after-env.js'],
  modulePathIgnorePatterns: [
    ".*__mocks__.*"
  ]
};

module.exports = {
  verbose: true,
  setupFilesAfterEnv: ['./jest-setup-files-after-env.js']
};
